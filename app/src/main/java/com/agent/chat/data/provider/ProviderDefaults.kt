package com.agent.chat.data.provider

/**
 * 新建 Provider 时的表单默认值（非运行时注入配置）。
 */
object ProviderDefaults {
    const val DEFAULT_BASE_URL = "https://api.openai.com/v1/"
    const val DEFAULT_MODEL_NAME = "gpt-4o-mini"
}
